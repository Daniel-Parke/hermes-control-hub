# Baseline

A minimal reference agent used for benchmarking. No sub-agents, no special
tooling, no memory — just the underlying model. Use it as the control when
measuring how much an agent's skills, tools, and memory lift performance.
