/**
 * OSS stub: `@agent-control-hub/mc-pro` is not shipped in public builds.
 * Commercial edition uses the real implementation in the private monorepo only.
 */
export function isCommercialLicenseValid(): boolean {
  return false;
}
