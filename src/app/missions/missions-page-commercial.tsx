"use client";

import MissionsPageOss from "./missions-page-oss";

/**
 * Commercial shell: currently the same UI as OSS; extend here for Taskforce / premium mission UX.
 */
export default function MissionsPageCommercial() {
  return <MissionsPageOss />;
}
