import type { TemplateDef } from "@/lib/mission-template-def";

/** OSS publish injects this stub so `mission-helpers` can import without the commercial data folder. */
export const TEMPLATES_COMMERCIAL_EXTRA: TemplateDef[] = [];
